ACLOCAL_AMFLAGS = -I m4

if BT_AUDIO_HAL_INTEGRATION
SUBDIRS = hci osi gki profile device btcore utils stack bta main tools audio_a2dp_hw
else
SUBDIRS = hci osi gki profile device btcore utils stack bta main tools
endif
